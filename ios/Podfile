# This file is automatically generated by Flutter.
# If you make changes to this file, they will be overwritten by Flutter.
# To maintain your changes, please use the Flutter-specific Podfile section at the end.

platform :ios, '13.0'

flutter_application_path = File.join(File.dirname(__FILE__), '..')
load File.join(flutter_application_path, '.flutter-app-tools', 'flutter_install_ios_pods.rb')

target 'Runner' do
  # Add your custom dependencies here.
  # For example:
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  
  # This line ensures all pods are using the correct deployment target.
  # It overrides any lower versions set in the pods' own specs.
  pre_install do |installer|
    installer.pod_targets.each do |pod|
      if pod.name == 'FirebaseCoreInternal'
        pod.swift_version = '5.0'
      end
      if pod.name == 'gRPC-C++'
        pod.swift_version = '5.0'
      end
      
      pod.platform.deployment_target = '13.0'
    end
  end
end
